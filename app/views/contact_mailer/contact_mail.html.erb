<h1>以下の業務で人員を募集します！</h1>

<h4>担当者: <%= @user.name %></h4>

<table>
  <thead>
    <tr>
      <th>件名</th>
      <th>業務内容</th>
      <th>開始日時</th>
      <th>終了日時</th>
      <th>募集人数</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user.blogs.last(1).each do |blog| %>
      <tr>
        <td><%= blog.title %></td>
        <td><%= blog.content %></td>
        <td><%= blog.starting_time.to_s(:starting_time)  %></td>
        <td><%= blog.finishing_time.to_s(:finishing_time) %></td>
        <td><%= blog.recruitment_number %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<h3>ご応募は以下のURLから応募者登録・応募者ログインして登録をお願いしますm(__)m</h3>
<p><%= link_to '応援募集一覧', recruit_blogs_url %></p>
<h5>※メールアドレス・パスワード共にご自身のメールアドレスをご入力ください</h5>